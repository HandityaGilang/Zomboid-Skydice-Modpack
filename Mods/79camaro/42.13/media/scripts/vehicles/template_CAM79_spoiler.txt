module Base
{
	
	model 79camaroSpoiler
	{
		mesh = vehicles/Vehicles_79camaro_Body|79camaro_spoiler0,
		shader = damn_vehicle_shader,
		static = FALSE,
		scale = 0.1,
	}

    model 79camaroSpoilerGhost
	{
		mesh = vehicles/Vehicles_79camaro_Body|79camaro_spoilerGhost,
		shader = damn_vehicle_shader,
		static = FALSE,
		scale = 0.1,
	}

	template vehicle CAM79SpoilerZ28
		{
		part DAMNSpoiler
       		{

            model Spoiler0
			{
                	file = 79camaroSpoiler,
			}

            model Spoiler1
			{
                	file = 79camaroSpoilerGhost,
			}

            category = bodywork,
            area = TruckBed,
            parent = TrunkDoor,
            itemType = Base.79camaroSpoiler;Base.79camaroGhostSpoiler,
            mechanicRequireKey = false,

            table install
            {
                items
                {
                    item
                    {
                        tags = Wrench,
                        count = 1,
                        keep = true,
                        equip = primary,
                    }
                }
                time = 1000,
                skills = Mechanics:4,
                recipes = Advanced Mechanics,
                test = Vehicles.InstallTest.Default,
                complete = CAM79.InstallComplete.Spoiler,
                requireInstalled = TrunkDoor,
                
            }
            table uninstall
            {
                items
                {
                    item
                    {
                        tags = Wrench,
                        count = 1,
                        keep = true,
                        equip = primary,
                    }
                }
                time = 1000,
                skills = Mechanics:4,
                recipes = Advanced Mechanics,
                test = Vehicles.UninstallTest.Default,
                complete = CAM79.UninstallComplete.Spoiler,
            }
            lua
            {
                init = CAM79.Init.Spoiler,
                create = CAM79.Create.Spoiler,
            }
        }
	}

    template vehicle CAM79SpoilerOFF
		{
            template! = CAM79SpoilerZ28,

		part DAMNSpoiler
       	{
            table install
            {
                complete = CAM79.InstallComplete.SpoilerOFF,
                
            }
            table uninstall
            {
                complete = CAM79.UninstallComplete.SpoilerOFF,
            }
            lua
            {
                init = CAM79.Init.SpoilerOFF,
                create = CAM79.Create.SpoilerOFF,
            }
        }
	}

    template vehicle CAM79SpoilerGhost
		{
            template! = CAM79SpoilerZ28,

		part DAMNSpoiler
       	{
            table install
            {
                complete = CAM79.InstallComplete.SpoilerGhost,
                
            }
            table uninstall
            {
                complete = CAM79.UninstallComplete.SpoilerGhost,
            }
            lua
            {
                init = CAM79.Init.SpoilerGhost,
                create = CAM79.Create.SpoilerGhost,
            }
        }
	}
}