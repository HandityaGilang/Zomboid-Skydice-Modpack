module Base
{

	model 78lamboCountachTrunkDoor
	{
		mesh = vehicles/Vehicles_78lamboCountach_Body|lp400_trunkLid_rear,
		shader = damn_vehicle_shader,
		static = FALSE,
		scale = 0.1,
	}

    model 78lamboCountachTrunkDoorFront
	{
		mesh = vehicles/Vehicles_78lamboCountach_Body|lp400_trunkLid_front,
		shader = damn_vehicle_shader,
		static = FALSE,
		scale = 0.1,
	}

	template vehicle LP400TrunkDoors
	{
		part TrunkDoor
		{

			model dmctrunkerony1639441
			{
				file = 78lamboCountachTrunkDoor,
			}

			anim Close
			{
				anim = trunk_opening,
				reverse = TRUE,
				rate = 1.0,
			}

			anim Open
			{
				anim = trunk_opening,
				rate = 1.0,
			}

			anim Closed
			{
				anim = trunk_opening,
				animate = FALSE,
			}

			anim Opened
			{
				anim = trunk_opening,
				reverse = TRUE,
				animate = FALSE,
			}

			category = bodywork,
			area = TruckBed,
			door
			{
			}
			anim Open
			{
				sound = VehicleTrunkOpenStandard,
			}
			anim Close
			{
				sound = VehicleTrunkCloseStandard,
			}
			anim Lock
			{
				sound = LockVehicleDoorStandard,
			}
			anim Unlock
			{
				sound = UnlockVehicleDoorStandard,
			}
			anim IsLocked
			{
				sound = VehicleDoorIsLockedStandard,
			}
			anim ActorOpen
			{
				anim = WindowOpenSuccess,
				rate = 0.15,
				angle = 0.0 0.0 0.0,
			}
			anim ActorClose
			{
				anim = Attack_Shove,
				rate = 0.3,
				angle = 0.0 0.0 0.0,
			}
			itemType = Base.78lamboCountachTrunkDoor,
			mechanicRequireKey = true,
            durability = 5,
			repairMechanic = true,
			table install
			{
				items
				{
					item
					{
						tags = Wrench,
						count = 1,
						keep = true,
						equip = primary,
					}
				}
				time = 500,
				skills = Mechanics:2,
				recipes = Intermediate Mechanics,
				test = Vehicles.InstallTest.Default,
				complete = Vehicles.InstallComplete.Door,
			}
			table uninstall
			{
				items
				{
					item
					{
						tags = Wrench,
						count = 1,
						keep = true,
						equip = primary,
					}
				}
				time = 500,
				skills = Mechanics:2,
				recipes = Intermediate Mechanics,
				test = Vehicles.UninstallTest.Default,
				complete = Vehicles.UninstallComplete.Door,
				requireUninstalled = DAMNSpoiler,

			}
			lua
			{
				create = Vehicles.Create.TrunkDoor,
				init = Vehicles.Init.Door,
				use = Vehicles.Use.TrunkDoor,
			}
		}

        part LP400TrunkDoorFront
		{

			model dmctrunkerony1639441231
			{
				file = 78lamboCountachTrunkDoorFront,
			}

			anim Close
			{
				anim = trunk_front_opening,
				reverse = TRUE,
				rate = 1.0,
			}

			anim Open
			{
				anim = trunk_front_opening,
				rate = 1.0,
			}

			anim Closed
			{
				anim = trunk_front_opening,
				animate = FALSE,
			}

			anim Opened
			{
				anim = trunk_front_opening,
				reverse = TRUE,
				animate = FALSE,
			}

			category = bodywork,
			area = Front,
			door
			{
			}
			anim Open
			{
				sound = VehicleTrunkOpenStandard,
			}
			anim Close
			{
				sound = VehicleTrunkCloseStandard,
			}
			anim Lock
			{
				sound = LockVehicleDoorStandard,
			}
			anim Unlock
			{
				sound = UnlockVehicleDoorStandard,
			}
			anim IsLocked
			{
				sound = VehicleDoorIsLockedStandard,
			}
			anim ActorOpen
			{
				anim = WindowOpenSuccess,
				rate = 0.15,
				angle = 0.0 270.0 0.0,
			}
			anim ActorClose
			{
				anim = Attack_Shove,
				rate = 0.3,
				angle = 0.0 270.0 0.0,
			}
			itemType = Base.78lamboCountachFrontTrunkDoor,
			mechanicRequireKey = true,
            durability = 5,
			repairMechanic = true,
			table install
			{
				items
				{
					item
					{
						tags = Wrench,
						count = 1,
						keep = true,
						equip = primary,
					}
				}
				time = 500,
				skills = Mechanics:2,
				recipes = Intermediate Mechanics,
				test = Vehicles.InstallTest.Default,
				complete = Vehicles.InstallComplete.Door,
			}
			table uninstall
			{
				items
				{
					item
					{
						tags = Wrench,
						count = 1,
						keep = true,
						equip = primary,
					}
				}
				time = 500,
				skills = Mechanics:2,
				recipes = Intermediate Mechanics,
				test = Vehicles.UninstallTest.Default,
				complete = Vehicles.UninstallComplete.Door,
				requireUninstalled = LP400SpoilerFront,

			}
			lua
			{
				create = Vehicles.Create.TrunkDoor,
				init = Vehicles.Init.Door,
				use = Vehicles.Use.TrunkDoor,
			}
		}
	}

    template vehicle LP400TrunkDoorsC
	{
        template! = LP400TrunkDoors,

        part TrunkDoor
		{
			table uninstall
			{
				test = DAMN.UninstallTest.Default,
				requireUninstalled = DAMNSpoiler;Radio,
			}
		}
	}
}

